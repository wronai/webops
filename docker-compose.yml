version: '3.8'

services:
  webops-voice:
    build:
      context: .
      dockerfile: Dockerfile.submodule
    container_name: webops-voice
    ports:
      - "8001:8001"  # API and web interface
    volumes:
      - ./logs:/app/logs
      - ./workspace:/app/workspace
      - /var/run/docker.sock:/var/run/docker.sock  # For Docker commands
      - ./uploads:/app/uploads
    group_add:
      - "127"
    environment:
      - PYTHONPATH=/app:/app/nlp2cmd-repo/src
      - NLP2CMD_HOST=0.0.0.0
      - NLP2CMD_PORT=8000
      - NLP2CMD_DEBUG=false
      - NLP2CMD_LOG_LEVEL=info
      - NLP2CMD_AUTO_EXECUTE=true
      - WORKSPACE_DIR=/app/workspace
      - NLP2CMD_USE_ENHANCED_CONTEXT=false
      - NLP2CMD_ENABLE_HEAVY_NLP=false
      - NLP2CMD_ENABLE_SPACY_LEMMATIZATION=false
      - NLP2CMD_MEASURE_RESOURCES=0
      - NLP2CMD_KEYWORD_DETECTOR_CONFIG=/app/nlp2cmd-repo/data/keyword_intent_detector_config.json
      - NLP2CMD_PATTERNS_FILE=/app/nlp2cmd-repo/data/patterns.json
    restart: unless-stopped
    networks:
      - webops-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Optional: Add reverse proxy for production
  nginx:
    image: nginx:alpine
    container_name: webops-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/ssl:/etc/nginx/ssl
      - ./logs/nginx:/var/log/nginx
    depends_on:
      - webops-voice
    networks:
      - webops-network
    restart: unless-stopped
    profiles:
      - production

networks:
  webops-network:
    driver: bridge

volumes:
  logs:
  workspace:
  uploads:
